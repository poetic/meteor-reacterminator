module.exports = function copyFontScriptsToMainHtml () {
  // get the index.html file
  // get the script tags
  // find the one using contains https://ajax.googleapis.com/ in source
  // find the one whose striped contents starts with WebFont.load
  // insert the tags into the the main.html
}
